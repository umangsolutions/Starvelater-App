<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.starvelater">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="m">
        <activity android:name=".activities.user.Qr_scan" />
        <activity android:name=".activities.corporate_cafe.All_CorporateCafe" />
        <activity android:name=".activities.college_canteen.All_CollegeCanteen" />
        <activity android:name=".activities.restaurant.All_Restaurants" />
        <activity android:name=".adapters.CustomGridLayout" />
        <activity android:name=".activities.restaurant.RestaurantProfile" />
        <activity android:name=".activities.restaurant.RestaurantProfileActivity" />
        <activity android:name=".activities.loginsignup.StartUpScreen" />
        <activity
            android:name=".activities.loginsignup.SignUpActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".activities.loginsignup.LoginActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".activities.user.AllCategories" />
        <activity android:name=".activities.general.OnBoarding" />
        <activity android:name=".activities.general.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.user.UserDashboard" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>